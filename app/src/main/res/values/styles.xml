<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="toolbarStyle">@style/textColorDark</item>
    </style>

    <style name="textColorDark" parent="@style/Widget.AppCompat.Toolbar">
        <item name="titleTextColor">#0e0e0e</item>
        <item name="colorControlNormal">#0e0e0e</item>
        <item name="android:textColorPrimaryInverse">#0e0e0e</item>
        <item name="android:textColorSecondary">#0e0e0e</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
